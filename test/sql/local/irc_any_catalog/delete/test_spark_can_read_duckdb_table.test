# name: test/sql/local/irc_any_catalog/delete/test_spark_can_read_duckdb_table.test
# description: Test iceberg deletes
# group: [delete]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
CALL enable_logging('HTTP');

statement ok
set logging_level='debug'

statement ok
DROP TABLE IF EXISTS my_datalake.default.duckdb_deletes_for_other_engines;

statement ok
CREATE TABLE my_datalake.default.duckdb_deletes_for_other_engines AS SELECT i a FROM range(10) t(i);

statement ok
INSERT INTO my_datalake.default.duckdb_deletes_for_other_engines SELECT i a FROM range(50, 60) t(i);

query I
DELETE FROM my_datalake.default.duckdb_deletes_for_other_engines WHERE a%2=0;
----
10

query II
SELECT COUNT(*), COUNT(*) FILTER(WHERE a%2=0) FROM my_datalake.default.duckdb_deletes_for_other_engines;
----
10	0


query I
select * from my_datalake.default.duckdb_deletes_for_other_engines order by all;
----
1
3
5
7
9
51
53
55
57
59
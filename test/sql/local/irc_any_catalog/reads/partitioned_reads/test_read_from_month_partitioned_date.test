# name: test/sql/local/irc_any_catalog/reads/partitioned_reads/test_read_from_month_partitioned_date.test
# group: [partitioned_reads]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

require icu

require ducklake

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
SET TimeZone = 'UTC';

statement ok
call enable_logging('http');

query I
select dt from my_datalake.default.month_partitioned_date where dt = DATE '2025-06-01';
----
2025-06-01

query I
select dt from my_datalake.default.hour_partitioned_timestamp where dt = '2025-01-01 00:00:00';
----
2025-01-01 00:00:00+00
2025-01-01 00:00:00+00
2025-01-01 00:00:00+00
2025-01-01 00:00:00+00

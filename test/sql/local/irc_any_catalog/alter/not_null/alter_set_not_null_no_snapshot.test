# name: test/sql/local/irc_any_catalog/alter/set_not_null/alter_set_not_null.test
# group: [set_not_null]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
CREATE SCHEMA IF NOT EXISTS my_datalake.default;

statement ok
drop table if exists my_datalake.default.my_table;

statement ok
CREATE TABLE my_datalake.default.my_table (a int);

statement ok
ALTER TABLE my_datalake.default.my_table ALTER COLUMN a SET NOT NULL;

statement error
INSERT INTO my_datalake.default.my_table VALUES (NULL);
----
Constraint Error: NOT NULL constraint failed: my_table.a

query I
FROM my_datalake.default.my_table;
----


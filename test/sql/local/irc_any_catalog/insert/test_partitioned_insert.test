# name: test/sql/local/irc_any_catalog/insert/test_partitioned_insert.test
# group: [insert]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
set logging_level='debug'

statement error
INSERT into my_datalake.default.insert_test_partitioned select
	'2010/06/11'::DATE a,
	42 b,
	'test' c
;
----
<REGEX>:.*Not implemented.*INSERT into a partitioned table is not supported yet.*

# name: test/sql/local/irc/test_row_lineage_read.test
# description: test integration with iceberg catalog read
# group: [irc]

require-env ICEBERG_SERVER_AVAILABLE

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
set logging_level='debug'

statement ok
CREATE SECRET (
    TYPE S3,
    KEY_ID 'admin',
    SECRET 'password',
    ENDPOINT '127.0.0.1:9000',
    URL_STYLE 'path',
    USE_SSL 0
);


statement ok
ATTACH '' AS my_datalake (
    TYPE ICEBERG,
    CLIENT_ID 'admin',
    CLIENT_SECRET 'password',
    ENDPOINT 'http://127.0.0.1:8181'
);

query II
select * from my_datalake.default.row_lineage_test order by data, id;
----
2	b_u1
4	d_u1
7	g_new
1	replaced
6	replaced

query III
select sequence_number, id, data from my_datalake.default.row_lineage_test order by data, id;
----
2	2	b_u1
2	4	d_u1
7	7	g_new
5	1	replaced
5	6	replaced

query IIII
select sequence_number, rowid, id, data from my_datalake.default.row_lineage_test where rowid < 11 order by data, id;
----
2	1	2	b_u1
2	3	4	d_u1
5	0	1	replaced
